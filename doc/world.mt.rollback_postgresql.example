# Example world.mt configuration for PostgreSQL rollback backend
#
# Note: rollback recording must also be enabled in minetest.conf:
#   enable_rollback_recording = true
#
# Pick the rollback backend for this world:
rollback_backend = postgresql
#
# Rollback PostgreSQL connection string. If unset, it falls back to pgsql_connection.
# For connection string format, see libpq connection string documentation.
pgsql_rollback_connection = host=127.0.0.1 port=5432 user=luanti dbname=luanti_rollback


